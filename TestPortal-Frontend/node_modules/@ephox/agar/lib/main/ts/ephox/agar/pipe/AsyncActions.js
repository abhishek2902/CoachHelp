const delay = (amount) => (next, _die) => {
    setTimeout(() => {
        next();
    }, amount);
};
// Not really async, but can fail.
const fail = (message) => (next, die) => {
    die('Fake failure: ' + message);
};
export { delay, fail };
//# sourceMappingURL=AsyncActions.js.map
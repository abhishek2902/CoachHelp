import { SugarElement } from '@ephox/sugar';
import * as fc from 'fast-check';
const createTag = (name) => {
    const partial = name.split('-');
    const tag = partial.length > 0 ? partial[0] : name;
    return SugarElement.fromTag(tag);
};
const comment = fc.string()
    .map((s) => {
    const raw = document.createComment(s);
    return SugarElement.fromDom(raw);
});
const elementOfArb = (arb) => arb.map((name) => createTag(name));
const elementOf = (tag) => createTag(tag);
const textOfArb = (arb) => arb.map(SugarElement.fromText);
const textOf = (s) => SugarElement.fromText(s);
export { elementOfArb, elementOf, comment, textOf, textOfArb };
//# sourceMappingURL=ArbNodes.js.map
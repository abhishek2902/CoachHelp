import * as fc from 'fast-check';
import * as WeightedChoice from './WeightedChoice';
const gOne = (wDecorations) => WeightedChoice.generator(wDecorations).chain((choice) => choice.fold(() => fc.constant({}), (c) => c.value.map((v) => {
    const r = {};
    r[c.property] = v;
    return r;
})));
const gEnforce = (decorations) => fc.constant(decorations);
export { gOne, gEnforce };
//# sourceMappingURL=TagDecorator.js.map
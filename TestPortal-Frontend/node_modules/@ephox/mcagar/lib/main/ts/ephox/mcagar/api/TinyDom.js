import { SimRange, SugarElement } from '@ephox/sugar';
/**
 * @deprecated Use SugarElement.fromDom instead.
 */
const fromDom = (elm) => SugarElement.fromDom(elm);
const fromRange = (rng) => SimRange.create(SugarElement.fromDom(rng.startContainer), rng.startOffset, SugarElement.fromDom(rng.endContainer), rng.endOffset);
const body = (editor) => SugarElement.fromDom(editor.getBody());
const document = (editor) => SugarElement.fromDom(editor.getDoc());
const documentElement = (editor) => SugarElement.fromDom(editor.getDoc().documentElement);
const container = (editor) => SugarElement.fromDom(editor.getContainer());
const contentAreaContainer = (editor) => SugarElement.fromDom(editor.getContentAreaContainer());
const targetElement = (editor) => SugarElement.fromDom(editor.getElement());
export const TinyDom = {
    fromDom,
    fromRange,
    body,
    container,
    contentAreaContainer,
    document,
    documentElement,
    targetElement
};
//# sourceMappingURL=TinyDom.js.map
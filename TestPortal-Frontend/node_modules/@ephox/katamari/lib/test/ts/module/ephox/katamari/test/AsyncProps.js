/* eslint-disable @tinymce/no-unimported-promise */
import { Assert } from '@ephox/bedrock-client';
import { Testable } from '@ephox/dispute';
// TODO: move to bedrock
export const eqAsync = (label, expected, actual, reject, testableA = Testable.tAny) => {
    try {
        Assert.eq(label, expected, actual, testableA);
    }
    catch (e) {
        reject(e);
    }
};
//# sourceMappingURL=AsyncProps.js.map
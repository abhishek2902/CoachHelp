export const addToStart = (str, prefix) => {
    return prefix + str;
};
export const addToEnd = (str, suffix) => {
    return str + suffix;
};
export const removeFromStart = (str, numChars) => {
    return str.substring(numChars);
};
export const removeFromEnd = (str, numChars) => {
    return str.substring(0, str.length - numChars);
};
//# sourceMappingURL=StrAppend.js.map
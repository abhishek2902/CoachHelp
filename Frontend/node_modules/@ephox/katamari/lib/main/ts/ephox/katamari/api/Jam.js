/** intersperseThunk :: ([a], (_ -> a)) -> [a] */
export const intersperseThunk = (array, delimiterThunk) => {
    if (array === undefined) {
        throw new Error('Cannot intersperse undefined');
    }
    if (array.length <= 1) {
        return array;
    }
    const r = [];
    r.push(array[0]);
    for (let i = 1; i < array.length; i++) {
        r.push(delimiterThunk());
        r.push(array[i]);
    }
    return r;
};
/** intersperse :: ([a], a) -> [a] */
export const intersperse = (array, delimiter) => {
    const thunk = () => {
        return delimiter;
    };
    return intersperseThunk(array, thunk);
};
//# sourceMappingURL=Jam.js.map
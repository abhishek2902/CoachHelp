import { PlatformDetection } from '@ephox/sand';
import { RealKeys } from './RealKeys';
import { Step } from './Step';
const platform = PlatformDetection.detect();
const pCopy = (selector) => {
    const modifiers = platform.os.isMacOS() ? { metaKey: true } : { ctrlKey: true };
    return RealKeys.pSendKeysOn(selector, [
        RealKeys.combo(modifiers, 'c')
    ]);
};
const sCopy = (selector) => Step.fromPromise(() => pCopy(selector));
const pPaste = (selector) => {
    const modifiers = platform.os.isMacOS() ? { metaKey: true } : { ctrlKey: true };
    return RealKeys.pSendKeysOn(selector, [
        RealKeys.combo(modifiers, 'v')
    ]);
};
const sPaste = (selector) => Step.fromPromise(() => pPaste(selector));
export { pCopy, pPaste, sCopy, sPaste };
//# sourceMappingURL=RealClipboard.js.map
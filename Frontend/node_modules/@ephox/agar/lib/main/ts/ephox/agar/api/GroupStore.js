import { Merger } from '@ephox/katamari';
export const GroupStore = () => {
    const data = {};
    const record = (prop, elem) => {
        let d = data[prop] !== undefined ? data[prop] : [];
        d = d.concat(elem);
        data[prop] = d;
    };
    const get = () => Merger.deepMerge({}, data);
    return {
        record,
        get
    };
};
//# sourceMappingURL=GroupStore.js.map
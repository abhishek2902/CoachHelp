import { Optional } from '@ephox/katamari';
import * as fc from 'fast-check';
export interface WeightedItem {
    useDepth?: boolean;
    weight: number;
}
export interface AccWeightItem {
    accWeight: number;
}
export interface WeightedList<T extends WeightedItem> {
    readonly list: (T & AccWeightItem)[];
    readonly total: number;
}
declare const generator: <T extends WeightedItem>(candidates: T[]) => fc.Arbitrary<Optional<T & AccWeightItem>>;
export { generator };
//# sourceMappingURL=WeightedChoice.d.ts.map
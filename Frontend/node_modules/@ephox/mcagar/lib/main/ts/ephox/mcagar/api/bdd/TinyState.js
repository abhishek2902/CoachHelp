const setEditableRoot = (editor, state) => {
    if (editor.setEditableRoot) {
        editor.setEditableRoot(state);
    }
    else {
        throw new Error('setEditableRoot requires at least TinyMCE v6.5');
    }
};
export const withNoneditableRootEditor = (editor, f) => {
    setEditableRoot(editor, false);
    f(editor);
    setEditableRoot(editor, true);
};
export const withNoneditableRootEditorAsync = async (editor, f) => {
    setEditableRoot(editor, false);
    await f(editor);
    setEditableRoot(editor, true);
};
//# sourceMappingURL=TinyState.js.map